import { VisibilityCheckDirective } from './visibility-check.directive';

describe('VisibilityCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new VisibilityCheckDirective();
    expect(directive).toBeTruthy();
  });
});
