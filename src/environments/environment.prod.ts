import { supportedLanguagesShared } from "./env-vars";

export const environment = {
  production: true,
  debugMode: false,
  redirect: true,
  supportedLanguages: supportedLanguagesShared,
  useDebugImage: false
};
