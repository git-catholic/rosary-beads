export const environment = {
  production: true,
  debugMode: false,
  redirect: true,
  supportedLanguages: [
    [ 'en', 'supported.english' ],
    // [ 'he', 'supported.hebrew', true ],
    [ 'es', 'supported.spanish' ]
  ],
  useDebugImage: false
};
